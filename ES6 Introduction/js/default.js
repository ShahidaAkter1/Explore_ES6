function add(first=98,second=0){
    const total=first + second;
    return total;
}

const result = add( );
console.log(result);

function name(first ='tua', last = 'akter'){
    const name=first + ' ' + last;
    return name;
}

// const fullname= name('shahida');
const fullname= name();
console.log(fullname);

const y = x => x*x; const z = y(5);
console.log(z);
const division= num1, num2 => console.log(num1 /num2)
const numbers=[10,20,30,40.50.60.70 ]
